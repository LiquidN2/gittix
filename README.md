# IMPORTANT: Environment Variables
+ For ```Test```, specify the variables in a ```.env``` file
+ For ```Dev```, specify the variables in the ```.yaml``` file. It's best to create k8s secrets and pass the secrets to the env via the ```.yaml``` file